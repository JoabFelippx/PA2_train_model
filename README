# Transfer Learning com VGG16

## Instalação:

    Instale as dependências listadas no arquivo requirements.txt
        ```
        pip install -r requirements.txt
        ```
    Defina o caminho do conjunto de dados na variável dataset_path no arquivo train.py.
